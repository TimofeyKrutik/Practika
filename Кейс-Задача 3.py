import random

def main():
    numbers = []  # список для хранения всех сгенерированных чисел
    
    while True:
        # Генерируем случайное число (например, от 1 до 100)
        rand_num = random.randint(1, 100)
        
        # Выводим сгенерированное число
        print(f"Сгенерировано число: {rand_num}")
        
        # Добавляем число в список
        numbers.append(rand_num)
        
        # Запрашиваем у пользователя ввод
        user_input = 5int(input("Введите 0 для выхода или любое другое число для продолжения: "))
        
        # Если пользователь ввёл 0, выходим из цикла
        if user_input == 0:
            break
    
    # Удаляем последний элемент (тот, который был сгенерирован перед вводом 0)
    if numbers:  # проверяем, что список не пуст
        numbers.pop()  # удаляем последний элемент
    
    # Выводим все числа, кроме последнего
    print("\nВсе сгенерированные числа (кроме последнего):")
    print(numbers if numbers else "Список пуст")

if __name__ == "__main__":
    main()


    